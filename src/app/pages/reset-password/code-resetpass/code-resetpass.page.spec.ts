import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CodeResetpassPage } from './code-resetpass.page';

describe('CodeResetpassPage', () => {
  let component: CodeResetpassPage;
  let fixture: ComponentFixture<CodeResetpassPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CodeResetpassPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
